apiVersion: v1
kind: Service

metadata:
  name: result-service
  labels:
    app: result-app

spec:
  selector:
    app: result-app

  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      
  type: ClusterIP