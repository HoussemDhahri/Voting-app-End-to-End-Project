apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: result-frontend
  namespace: monitoring
  labels:
    release: monitoring

spec:
  jobName: result-frontend-blackbox

  interval: 30s
  scrapeTimeout: 10s

  module: http_2xx

  prober:
    url: blackbox-exporter-prometheus-blackbox-exporter.monitoring.svc:9115
    scheme: http

  targets:
    staticConfig:
      static:
        - http://result-service.voting-staging.svc:4000/
        - http://result-service.voting-prod.svc:4000/