apiVersion: v1
kind: Service

metadata:
  name: pgadmin-service
  labels:
    app: pgadmin-app

spec:
  selector:
    app: pgadmin-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      
  type: ClusterIP