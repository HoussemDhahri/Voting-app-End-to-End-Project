apiVersion: v1
kind: Service

metadata:
  name: redisinsight-service
  labels:
    app: redisinsight-app

spec:
  selector:
    app: redisinsight-app

  ports:
    - protocol: TCP
      port: 5540
      targetPort: 5540

  type: ClusterIP